services:
  netgazer:
    build: .
    container_name: netgazer
    restart: unless-stopped
    ports:
      - "9527:9527"     # HTTP + gRPC（单端口，通过 HTTP/2 路径区分）
    volumes:
      - netgazer_data:/var/lib/netgazer
    environment:
      - TZ=Asia/Shanghai

volumes:
  netgazer_data:
